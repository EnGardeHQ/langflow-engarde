# Dockerfile for EasyAppointments
# This Dockerfile is used for Railway deployment

FROM easyappointments/easyappointments:latest

# Set working directory
WORKDIR /var/www/html

# Expose port 80
EXPOSE 80

# The base image already includes Apache and PHP
# No additional configuration needed as Railway will handle environment variables
